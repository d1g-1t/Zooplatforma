# 🐾 Zooplatforma# 🐾 Zooplatforma# Zooplatforma# Zooplatforma# Zooplatforma



Платформа для помощи животным - современное веб-приложение, объединяющее приюты, волонтёров и благотворительные организации для помощи бездомным животным.



## ✨ Возможности**Платформа для помощи животным** - современное веб-приложение, объединяющее приюты, волонтёров и благотворительные организации для помощи бездомным животным.



- 📢 Объявления о животных, нуждающихся в помощи

- 🏥 Медицинские карты с историей лечения и вакцинации

- 👥 Профили кураторов и владельцев## ✨ ВозможностиПлатформа для помощи животным, объединяющая приюты, волонтёров и благотворительные организации.

- 💬 Система комментариев и лайков

- 📸 Галерея фотографий животных

- 🔔 Уведомления о статусе заявок

- 📊 Админ-панель для управления контентом- 📢 **Объявления** о животных, нуждающихся в помощи



## 🛠 Стек технологий- 🏥 **Медицинские карты** с историей лечения и вакцинации



### Backend- 👥 **Профили кураторов** и владельцев## Стек технологийПлатформа для помощи животным, объединяющая приюты, волонтёров и благотворительные организации.Платформа для помощи животным, объединяющая приюты, волонтёров и благотворительные организации.

- Python 3.12

- Django 5- 💬 **Система комментариев** и лайков

- Django REST Framework

- PostgreSQL 15- 📸 **Галерея фотографий** животных

- Redis 7

- Celery- 🔔 **Уведомления** о статусе заявок

- Gunicorn

- 📊 **Админ-панель** для управления контентом- **Backend:** Python 3.12, Django 5, DRF, Celery, PostgreSQL, Redis

### Frontend

- React 18

- TypeScript

- Vite## 🛠 Стек технологий- **Frontend:** React 18, Vite, TypeScript, SCSS

- SCSS



### DevOps

- Docker & Docker Compose### Backend- **Инфраструктура:** Docker, Docker Compose, Makefile, Nginx, Gunicorn## Стек технологий## Стек

- Nginx

- Makefile- **Python 3.12** - современная версия Python



## 🚀 Быстрый старт- **Django 5** - мощный веб-фреймворк



### Требования- **Django REST Framework** - для REST API



- Docker 24.0+- **PostgreSQL 15** - надёжная СУБД## Быстрый старт

- Docker Compose 2.0+

- Make- **Redis 7** - кеширование и брокер сообщений

- Git

- **Celery** - асинхронные задачи

### Установка за один шаг

- **Gunicorn** - WSGI сервер

```bash

git clone https://github.com/d1g-1t/Zooplatforma.git### Первый запуск проекта- **Backend:** Python 3.12, Django 5, DRF, Celery, PostgreSQL, Redis- **Backend:** Python 3.12, Django 5, DRF, Celery, PostgreSQL, Redis

cd Zooplatforma

make setup### Frontend

```

- **React 18** - современная библиотека для UI

Эта команда автоматически:

1. Очистит старые контейнеры- **TypeScript** - типизированный JavaScript

2. Создаст файл `.env` из шаблона

3. Соберёт Docker образы- **Vite** - быстрая сборкаПосле клонирования репозитория выполните одну команду:- **Frontend:** React 18, Vite, TypeScript, SCSS- **Frontend:** React 18, Vite, TypeScript, SCSS

4. Запустит все сервисы

5. Применит миграции базы данных- **SCSS** - препроцессор CSS

6. Создаст суперпользователя

7. Соберёт статические файлы



Время установки: ~5-10 минут### DevOps



## 🌐 Доступ к приложению- **Docker** & **Docker Compose** - контейнеризация```bash- **Инфраструктура:** Docker, docker compose, Makefile, Nginx, Gunicorn- **Инфраструктура:** Docker, docker compose, Makefile, Nginx, Gunicorn



После успешного запуска:- **Nginx** - веб-сервер



| Сервис | URL | Описание |- **Makefile** - автоматизация задачmake setup

|--------|-----|----------|

| Frontend | http://localhost:8080 | Основное приложение |

| Backend API | http://localhost:8000/api/v1/ | REST API |

| Admin Panel | http://localhost:8000/admin | Панель администратора |## 🚀 Быстрый старт```



### Учётные данные по умолчанию



- Email: `admin@admin.ru`### Требования

- Пароль: `admin777`



> ⚠️ Измените эти данные в production!

- **Docker** 24.0+ и **Docker Compose** 2.0+Эта команда автоматически:## Быстрый старт## Быстрый запуск

## 📝 Основные команды

- **Make** (обычно предустановлен)

### Управление проектом

- **Git**- Очистит старые контейнеры (если есть)

```bash

make setup          # Полная установка с нуля

make init           # Алиас для setup

make up             # Запустить все сервисы### Установка за один шаг- Создаст файл `.env` из шаблона

make down           # Остановить все сервисы

make restart        # Перезапустить все сервисы

make clean          # Удалить все контейнеры и volumes

``````bash- Соберёт Docker образы



### Мониторингgit clone https://github.com/d1g-1t/Zooplatforma.git



```bashcd Zooplatforma- Запустит все сервисы### Первый запуск проекта1. Создайте файл `zooplatforma_backend/.env` (команда `make ensure-env` автоматически скопирует `zooplatforma_backend/.env.example`, после чего можно отредактировать значения при необходимости).

make logs           # Просмотр логов всех сервисов

make status         # Проверить статус контейнеровmake setup

make build          # Пересобрать Docker образы

make test-stack     # Протестировать доступность сервисов```- Применит миграции базы данных

```



### Разработка

Эта команда автоматически:- Создаст суперпользователя2. Выполните демонстрационный запуск одной командой:

```bash

make backend-shell   # Войти в shell контейнера backend1. ✅ Очистит старые контейнеры (если есть)

make frontend-shell  # Войти в shell контейнера frontend

make migrate         # Применить миграции базы данных2. ✅ Создаст файл `.env` из шаблона- Соберёт статические файлы

make demo            # Запустить в foreground режиме

```3. ✅ Соберёт Docker образы



## 📁 Структура проекта4. ✅ Запустит все сервисыПосле клонирования репозитория выполните одну команду:



```5. ✅ Применит миграции базы данных

Zooplatforma/

├── docker-compose.yml           # Конфигурация Docker сервисов6. ✅ Создаст суперпользователя### Доступ к сервисам

├── Makefile                     # Команды автоматизации

├── README.md                    # Документация7. ✅ Соберёт статические файлы

│

├── zooplatforma_backend/        # Django Backend```bash

│   ├── api/                     # REST API endpoints

│   │   └── v1/                  # API версия 1**Время установки:** ~5-10 минут (в зависимости от скорости интернета)

│   ├── apps/                    # Django приложения

│   │   ├── announcements/       # Объявления о животныхПосле успешного запуска сервисы доступны по адресам:

│   │   ├── pets/                # Управление животными

│   │   └── users/               # Пользователи### Альтернативный способ

│   ├── config/                  # Настройки Django

│   ├── templates/               # Email шаблоны```bashmake demo

│   ├── tests/                   # Тесты

│   ├── Dockerfile```bash

│   ├── entrypoint.sh

│   ├── requirements.txt./quickstart.sh- **Backend API:** http://localhost:8000/api/v1/

│   └── .env.example

│```

└── zooplatforma_frontend/       # React Frontend

    ├── src/- **Admin панель:** http://localhost:8000/adminmake setup```

    │   ├── api/

    │   ├── pages/## 🌐 Доступ к приложению

    │   ├── features/

    │   ├── shared/- **Frontend:** http://localhost:8080

    │   ├── widgets/

    │   └── store/После успешного запуска сервисы доступны по адресам:

    ├── public/

    ├── Dockerfile```

    ├── package.json

    └── vite.config.ts| Сервис | URL | Описание |

```

|--------|-----|----------|### Учётные данные администратора

## ⚙️ Конфигурация

| 🖥 **Frontend** | http://localhost:8080 | Основное приложение |

### Переменные окружения

| 🔌 **Backend API** | http://localhost:8000/api/v1/ | REST API |Команда соберёт образы, применит миграции и запустит все сервисы в foreground-режиме. Остановить сервисы можно сочетанием `Ctrl+C`.

Основные переменные в `zooplatforma_backend/.env`:

| 👤 **Admin Panel** | http://localhost:8000/admin | Панель администратора |

```env

POSTGRES_DB=zoo_projectПо умолчанию (из `.env.example`):

POSTGRES_USER=zoo_user

POSTGRES_PASSWORD=zoo_password### Учётные данные по умолчанию



SECRET_KEY=your-secret-key-here- Email: `admin@admin.ru`Эта команда автоматически:

DEBUG_VALUE=True

ALLOWED_HOSTS=127.0.0.1,localhost- **Email:** `admin@admin.ru`



SUPERUSER_EMAIL=admin@admin.ru- **Пароль:** `admin777`- Пароль: `admin777`

SUPERUSER_PASSWORD=admin777

SUPERUSER_PHONE=79772227788



REDIS_URL=redis://redis:6379/1> ⚠️ **Важно:** Измените эти данные в production!- Очистит старые контейнеры (если есть)**Или запустите в фоновом режиме:**

CELERY_BROKER_URL=redis://redis:6379/0

CELERY_RESULT_BACKEND=redis://redis:6379/0

```

## 📝 Основные команды## Основные команды

После редактирования `.env`:



```bash

make restart### Управление проектом- Создаст файл `.env` из шаблона

```



## 🧪 Разработка

```bash### Управление проектом

### Backend локально

make setup          # 🚀 Полная установка с нуля (первый запуск)

```bash

cd zooplatforma_backendmake init           # 🔄 Алиас для setup- Соберёт Docker образы```bash

python -m venv venv

source venv/bin/activatemake up             # ▶️  Запустить все сервисы

pip install -r requirements.txt

python manage.py migratemake down           # ⏹️  Остановить все сервисы```bash

python manage.py createsuperuser

python manage.py runservermake restart        # 🔄 Перезапустить все сервисы

```

make clean          # 🗑️  Удалить все контейнеры и volumesmake setup          # Полная установка с нуля (рекомендуется для первого запуска)- Запустит все сервисыmake up

### Frontend локально

```

```bash

cd zooplatforma_frontendmake init           # Алиас для setup

npm install

npm run dev### Мониторинг и отладка

```

make clean          # Остановить и удалить все контейнеры и volumes- Применит миграции базы данных```

### Тестирование

```bash

```bash

cd zooplatforma_backend && pytestmake logs           # 📋 Просмотр логов всех сервисовmake build          # Пересобрать Docker образы

cd zooplatforma_frontend && npm test

```make status         # 📊 Проверить статус контейнеров



## 🐛 Устранение проблемmake health         # 🏥 Проверить доступность сервисовmake up             # Запустить все сервисы в фоновом режиме- Соберёт статические файлы



### Порт уже занятmake build          # 🔨 Пересобрать Docker образы



```bash```make down           # Остановить все сервисы

make clean

make setup

```

### Разработкаmake restart        # Перезапустить все сервисы**Проверка работоспособности:**

### Проблемы с базой данных



```bash

make down```bashmake logs           # Просмотр логов всех сервисов

docker volume rm zooplatforma-1_postgres_data

make setupmake backend-shell   # 💻 Войти в shell контейнера backend

```

make frontend-shell  # 💻 Войти в shell контейнера frontendmake status         # Проверить статус контейнеров### Доступ к сервисам

### Контейнер не запускается

make migrate         # 🗄️  Применить миграции базы данных

```bash

docker compose logs <service-name>make test-stack      # 🧪 Протестировать доступность всех сервисовmake health         # Проверить доступность сервисов

make clean

make build```

make up

`````````bash



### Проверка статуса### Демонстрационный режим



```bash

make status

docker compose ps```bash

```

make demo           # 🎬 Запустить в foreground (Ctrl+C для остановки)### РазработкаПосле успешного запуска сервисы доступны по адресам:make test-stack

## 📊 Мониторинг

```

### Просмотр логов



```bash

make logs## 📁 Структура проекта



docker compose logs backend -f```bash```

docker compose logs frontend -f

docker compose logs db -f```

```

Zooplatforma/make backend-shell   # Войти в shell контейнера backend

### Проверка работоспособности

├── 📄 docker-compose.yml           # Конфигурация Docker сервисов

```bash

make test-stack├── 📄 Makefile                     # Команды автоматизацииmake frontend-shell  # Войти в shell контейнера frontend- **Backend API:** http://localhost:8000/api/v1/



curl http://localhost:8000/api/v1/├── 📄 README.md                    # Документация

curl http://localhost:8080

```├── 🔧 healthcheck.sh               # Скрипт проверки здоровьяmake migrate         # Применить миграции базы данных



## 🚢 Production├── 🔧 quickstart.sh                # Быстрый старт



Для production:│make test-stack      # Протестировать доступность всех сервисов- **Admin панель:** http://localhost:8000/adminЭта команда запустит стек и выполнит базовые проверки доступности сервисов.



```bash├── 🐍 zooplatforma_backend/        # Django Backend

cd zooplatforma_backend

docker compose -f docker-compose.production.yml up -d│   ├── 📂 api/                     # REST API endpoints```

```

│   │   └── v1/                     # API версия 1

Важные изменения для production:

- Установите `DEBUG_VALUE=False`│   ├── 📂 apps/                    # Django приложения- **Frontend:** http://localhost:8080

- Смените `SECRET_KEY`

- Настройте HTTPS│   │   ├── announcements/          # Объявления о животных

- Настройте SMTP для email

- Используйте сильные пароли│   │   ├── pets/                   # Управление животными### Демонстрационный режим

- Настройте backup базы данных

│   │   └── users/                  # Пользователи и аутентификация

## 📄 Лицензия

│   ├── 📂 config/                  # Настройки Django## Ежедневные команды Makefile

MIT License

│   ├── 📂 templates/               # Email шаблоны

---

│   ├── 📂 tests/                   # Тесты```bash

<p align="center">

Сделано с ❤️ для помощи животным│   ├── 🐳 Dockerfile               # Docker образ backend

</p>

│   ├── 🔧 entrypoint.sh            # Скрипт запускаmake demo           # Запустить в foreground-режиме (остановка через Ctrl+C)### Учётные данные администратора

│   ├── 📄 requirements.txt         # Python зависимости

│   └── 📄 .env.example             # Шаблон переменных окружения```

│

└── ⚛️  zooplatforma_frontend/      # React Frontend```bash

    ├── 📂 src/

    │   ├── api/                    # API клиенты## Структура проекта

    │   ├── pages/                  # Страницы приложения

    │   ├── features/               # Функциональные компонентыПо умолчанию (из `.env.example`):make build       # пересобрать образы

    │   ├── shared/                 # Общие компоненты

    │   ├── widgets/                # Виджеты```

    │   └── store/                  # Redux store

    ├── 📂 public/                  # Статические файлы.- Email: `admin@admin.ru`make up          # запустить стек в фоне

    ├── 🐳 Dockerfile               # Docker образ frontend

    ├── 📄 package.json             # npm зависимости├── docker-compose.yml           # Конфигурация Docker сервисов

    └── ⚙️  vite.config.ts           # Конфигурация Vite

```├── Makefile                     # Команды для управления проектом- Пароль: `admin777`make down        # остановить и очистить контейнеры



## ⚙️ Конфигурация├── healthcheck.sh               # Скрипт проверки доступности сервисов



### Переменные окружения├── zooplatforma_backend/        # Django backendmake logs        # посмотреть логи



Основные переменные в `zooplatforma_backend/.env`:│   ├── api/                     # REST API endpoints



```env│   ├── apps/                    # Django приложения## Основные командыmake restart     # перезапустить стек

# База данных

POSTGRES_DB=zoo_project│   │   ├── announcements/       # Объявления

POSTGRES_USER=zoo_user

POSTGRES_PASSWORD=zoo_password│   │   ├── pets/                # Животныеmake test-stack  # запустить и протестировать все сервисы



# Django│   │   └── users/               # Пользователи

SECRET_KEY=your-secret-key-here

DEBUG_VALUE=True│   ├── config/                  # Настройки Django### Управление проектом```

ALLOWED_HOSTS=127.0.0.1,localhost

DOMAIN=http://localhost:8000│   ├── templates/               # Email шаблоны



# Суперпользователь (создаётся автоматически)│   ├── Dockerfile

SUPERUSER_EMAIL=admin@admin.ru

SUPERUSER_PASSWORD=admin777│   ├── entrypoint.sh

SUPERUSER_PHONE=79772227788

SUPERUSER_FIRST_NAME=Admin│   ├── requirements.txt```bash## Точки доступа



# Redis и Celery│   └── .env.example             # Шаблон переменных окружения

REDIS_URL=redis://redis:6379/1

CELERY_BROKER_URL=redis://redis:6379/0└── zooplatforma_frontend/       # React frontendmake setup          # Полная установка с нуля (рекомендуется для первого запуска)

CELERY_RESULT_BACKEND=redis://redis:6379/0

    ├── src/

# Email (для production настройте SMTP)

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend    │   ├── api/                 # API клиентыmake init           # Алиас для setup- Backend: http://localhost:8000

EMAIL_HOST=localhost

EMAIL_PORT=1025    │   ├── pages/               # Страницы приложения

```

    │   ├── features/            # Функциональные компонентыmake clean          # Остановить и удалить все контейнеры и volumes- Frontend (статическая сборка): http://localhost:8080

### Изменение конфигурации

    │   ├── shared/              # Общие компоненты

После редактирования `.env` перезапустите сервисы:

    │   └── widgets/             # Виджетыmake build          # Пересобрать Docker образы- API: http://localhost:8000/api/v1/

```bash

make restart    ├── Dockerfile

```

    └── package.jsonmake up             # Запустить все сервисы в фоновом режиме

## 🧪 Разработка

```

### Backend (локально без Docker)

make down           # Остановить все сервисы## Тесты

```bash

cd zooplatforma_backend## Переменные окружения

python -m venv venv

source venv/bin/activate  # Windows: venv\Scripts\activatemake restart        # Перезапустить все сервисы

pip install -r requirements.txt

python manage.py migrateОсновные переменные в `zooplatforma_backend/.env`:

python manage.py createsuperuser

python manage.py runservermake logs           # Просмотр логов всех сервисов```bash

```

```env

### Frontend (локально без Docker)

# База данных```cd zooplatforma_backend && poetry run pytest

```bash

cd zooplatforma_frontendPOSTGRES_DB=zoo_project

npm install

npm run devPOSTGRES_USER=zoo_usercd ../zooplatforma_frontend && npm test

```

POSTGRES_PASSWORD=zoo_password

### Тестирование

### Разработка```

```bash

# Backend тесты# Django

cd zooplatforma_backend && pytest

SECRET_KEY=your-secret-key-here

# Frontend тесты

cd zooplatforma_frontend && npm testDEBUG_VALUE=True```bash

```

ALLOWED_HOSTS=127.0.0.1,localhostmake backend-shell   # Войти в shell контейнера backend

## 🐛 Устранение проблем

make frontend-shell  # Войти в shell контейнера frontend

### Порт уже занят

# Суперпользовательmake migrate         # Применить миграции базы данных

```bash

make cleanSUPERUSER_EMAIL=admin@admin.rumake test-stack      # Протестировать доступность всех сервисов

make setup

```SUPERUSER_PASSWORD=admin777```



### Проблемы с базой данныхSUPERUSER_PHONE=79772227788



```bashSUPERUSER_FIRST_NAME=Admin### Демонстрационный режим

make down

docker volume rm zooplatforma-1_postgres_data

make setup

```# Redis и Celery```bash



### Контейнер не запускаетсяREDIS_URL=redis://redis:6379/1make demo           # Запустить в foreground-режиме (остановка через Ctrl+C)



```bashCELERY_BROKER_URL=redis://redis:6379/0```

# Проверить логи

docker compose logs <service-name>CELERY_RESULT_BACKEND=redis://redis:6379/0



# Или конкретный сервис```## Структура проекта

make logs



# Пересобрать и запустить заново

make cleanПосле первого запуска можно отредактировать `.env` файл и перезапустить проект:```

make build

make up.

```

```bash├── docker-compose.yml           # Конфигурация Docker сервисов

### Проверка статуса

make restart├── Makefile                     # Команды для управления проектом

```bash

make status         # Статус контейнеров```├── zooplatforma_backend/        # Django backend

make health         # HTTP endpoints

docker compose ps   # Детальная информация│   ├── api/                     # REST API endpoints

```

## Разработка│   ├── apps/                    # Django приложения

## 📊 Мониторинг

│   ├── config/                  # Настройки Django

### Просмотр логов

### Backend (локально без Docker)│   ├── templates/               # Email шаблоны

```bash

# Все сервисы│   ├── Dockerfile

make logs

```bash│   ├── entrypoint.sh

# Конкретный сервис

docker compose logs backend -fcd zooplatforma_backend│   ├── requirements.txt

docker compose logs frontend -f

docker compose logs db -fpython -m venv venv│   └── .env.example             # Шаблон переменных окружения

```

source venv/bin/activate  # На Windows: venv\Scripts\activate└── zooplatforma_frontend/       # React frontend

### Проверка здоровья

pip install -r requirements.txt    ├── src/

```bash

# Автоматическая проверкаpython manage.py migrate    ├── Dockerfile

make health

python manage.py createsuperuser    └── package.json

# Ручная проверка

curl http://localhost:8000/api/v1/python manage.py runserver```

curl http://localhost:8080

``````



## 🚢 Production развертывание## Переменные окружения



Для production используйте специальную конфигурацию:### Frontend (локально без Docker)



```bashОсновные переменные в `zooplatforma_backend/.env`:

cd zooplatforma_backend

docker compose -f docker-compose.production.yml up -d```bash

```

cd zooplatforma_frontend```env

**Важные изменения для production:**

- Установите `DEBUG_VALUE=False`npm install# База данных

- Смените `SECRET_KEY`

- Настройте HTTPSnpm run devPOSTGRES_DB=zoo_project

- Настройте SMTP для email

- Используйте сильные пароли```POSTGRES_USER=zoo_user

- Настройте backup базы данных

POSTGRES_PASSWORD=zoo_password

## 🤝 Вклад в проект

## Тестирование

Мы приветствуем вклад в развитие проекта! 

# Django

1. Fork репозитория

2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)```bashSECRET_KEY=your-secret-key

3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)

4. Push в branch (`git push origin feature/AmazingFeature`)cd zooplatforma_backend && pytestDEBUG_VALUE=True

5. Откройте Pull Request

cd zooplatforma_frontend && npm testALLOWED_HOSTS=127.0.0.1,localhost

## 📞 Поддержка

```

Если у вас возникли вопросы или проблемы:

# Суперпользователь

- 📧 Email: support@zooplatforma.ru

- 🐛 Issues: [GitHub Issues](https://github.com/d1g-1t/Zooplatforma/issues)## Устранение проблемSUPERUSER_EMAIL=admin@admin.ru

- 💬 Discussions: [GitHub Discussions](https://github.com/d1g-1t/Zooplatforma/discussions)

SUPERUSER_PASSWORD=admin777

## 📄 Лицензия

### Порт уже занят```

MIT License - см. файл [LICENSE](LICENSE) для деталей



---

Если появляется ошибка "port is already allocated":После первого запуска можно отредактировать `.env` файл и перезапустить проект:

<p align="center">

  Сделано с ❤️ для помощи животным

</p>

```bash```bash

make cleanmake restart

make setup```

```

## Разработка

### Проблемы с базой данных

### Backend

```bash

make down```bash

docker volume rm zooplatforma-1_postgres_datacd zooplatforma_backend

make setuppython -m venv venv

```source venv/bin/activate  # На Windows: venv\Scripts\activate

pip install -r requirements.txt

### Проверка статуса сервисовpython manage.py runserver

```

```bash

make status         # Статус контейнеров### Frontend

make health         # Проверка доступности HTTP endpoints

docker compose logs backend --tail 50```bash

```cd zooplatforma_frontend

npm install

### Контейнер не запускаетсяnpm run dev

```

```bash

docker compose logs <service-name>## Тестирование

make clean

make build```bash

make upcd zooplatforma_backend && pytest

```cd zooplatforma_frontend && npm test

```

## Производственное развертывание

## Устранение проблем

Для production используйте отдельные конфигурации:

### Порт уже занят

```bash

cd zooplatforma_backendЕсли появляется ошибка "port is already allocated":

docker compose -f docker-compose.production.yml up -d

``````bash

make clean

## Лицензияmake setup

```

MIT

### Проблемы с базой данных

```bash
make down
docker volume rm zooplatforma-1_postgres_data
make setup
```

### Проверка статуса сервисов

```bash
docker compose ps
make test-stack
```

## Лицензия

MIT
